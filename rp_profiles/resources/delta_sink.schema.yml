resources:
  schemas:
    sink_schema:
      name: kafka_delta_sinks
      grants:
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
            - ALL_PRIVILEGES
      catalog_name: ${var.catalog}