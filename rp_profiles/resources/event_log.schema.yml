resources:
  schemas:
    event_log_schema:
      name: ${var.event_log_schema}
      grants:
        - principal: ${var.run_as_user}
          privileges:
            - MANAGE
            - ALL_PRIVILEGES
      catalog_name: ${var.event_log_catalog}