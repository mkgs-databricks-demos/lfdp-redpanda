resources:
  pipelines:
    rp_cloud_quickstart_pipeline:
      name: rp_cloud_quickstart_pipeline
      continuous: false
      serverless: true
      channel: "PREVIEW"
      edition: ADVANCED
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "."
      libraries:
        - glob:
            include: /Workspace/Users/matthew.giglia@databricks.com/lfdp-redpanda/rp_cloud_quickstart/resources/rp_cloud_quickstart_pipeline/transformations/profiles_bronze.py
        - glob:
            include: /Workspace/Users/matthew.giglia@databricks.com/lfdp-redpanda/rp_cloud_quickstart/resources/rp_cloud_quickstart_pipeline/transformations/profiles.sql
        - glob:
            include: /Workspace/Users/matthew.giglia@databricks.com/lfdp-redpanda/rp_cloud_quickstart/resources/rp_cloud_quickstart_pipeline/transformations/profiles_cdf.sql
      configuration:
        secret_scope: matthew-giglia
        secret_key_user: redpanda_cloud_user
        secret_key_password: redpanda_cloud_password
        secret_key_bootstrap_server: redpanda_cloud_bootstrap_server
        spark.databricks.streaming.realTimeMode.enabled: "true"
      event_log:
        schema: ${var.event_log_schema}
        catalog: ${var.event_log_catalog}
